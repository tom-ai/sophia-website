export type PostProps = {
  id: string;
  date: string;
  message: string;
  link: string;
  collaborators: any;
};

export default function Post({ post }: { post: PostProps }) {
  return (
    
  );
}
